# Coding Agent System

Автоматизированная агентная система для полного цикла разработки (SDLC) в GitHub.

## 🚀 Быстрый старт

### Локальный запуск
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ninkkka/coding-agent.git
cd coding-agent

# 2. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env файл (добавьте токены)

# 3. Запустите через Docker
docker-compose up -d

# 4. Или установите зависимости и запустите локально
pip install -r requirements.txt
python src/agents/coding_agent.py --issue 1 --repo ninkkka/coding-agent
